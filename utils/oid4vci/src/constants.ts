export const DEFAULT_POLLING_INTERVAL_SECONDS = 5;

export const WELL_KNOWN_ENDPOINTS = {
  CREDENTIAL_ISSUER_METADATA: '.well-known/openid-credential-issuer',
  OPENID_PROVIDER_CONFIGURATION: '.well-known/openid-configuration',
  OAUTH_SERVER_METADATA: '.well-known/oauth-authorization-server',
  JWT_ISSUER_METADATA: '.well-known/jwt-issuer',
};
